# Message Spam Filtering 

Classifies SMS messages as spam or ham (not spam) using NLP preprocessing and machine learning. 

# Lesson Learned
Text is cleaned by removing stopwords and applying stemming, then vectorized with TF-IDF. Multiple classifiers (SVM, Naive Bayes, Decision Tree) are trained and evaluated.